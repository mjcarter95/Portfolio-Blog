---
const { tone = "light", id } = Astro.props;
/* tone: "light" (white) | "alt" (light gray) */
const bg = tone === "alt" ? "bg-gray-50" : "bg-white";
---
<section id={id} class={`${bg}`}>
  <div class="max-w-6xl mx-auto px-6 py-24">
    <slot />
  </div>
</section>
